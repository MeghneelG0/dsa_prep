#include <bits/stdc++.h>
using namespace std; 

/* Goal: Find the largest subset sum that is <= 11 (Target) 
Input: [1, 6, 11, 5].
Target: 11.



*/

int solve(vector<int>& nums, int idx, int target) {
    vector<int> ds; 
    if (idx == target) {
        ds.push_back()
    }
}